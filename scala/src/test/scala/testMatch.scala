import nebula._

import org.scalatest.FunSuite
 
class TestPyramidHistogram extends FunSuite {
  // import PyramidHistogram._
  
  // val xPoint0 = IndexedSeq(0, 0)
  // val xPoint1 = IndexedSeq(0, 0)
  // val xPoint2 = IndexedSeq(2, 2)
  // val yPoint0 = IndexedSeq(0, 0)
  // val yPoint1 = IndexedSeq(2, 0)
  // val yPoint2 = IndexedSeq(0, 1)
  
  // val box = Box(IndexedSeq((0, 2), (0, 2)))
  
  // test("distance") {
  //   val x = create(box)(List(xPoint0, xPoint1, xPoint2))
  //   val y = create(box)(List(yPoint0, yPoint1, yPoint2))
  //   assert(distance(x, y) === 8)
  // }
}
